import { Route } from '@angular/router';

import { TermConditionComponent } from './index';

export const TermConditionRoutes: Route[] = [
	{
		path: 'terms-conditions',
		component: TermConditionComponent,
	}
];
