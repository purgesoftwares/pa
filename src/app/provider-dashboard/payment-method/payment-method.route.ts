import { Route } from '@angular/router';

import { PaymentMethodComponent } from './index';

export const PaymentMethodRoutes: Route[] = [
	{
		path: 'payment-method',
		component: PaymentMethodComponent,
	}
];
