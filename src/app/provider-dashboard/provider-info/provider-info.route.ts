import { Route } from '@angular/router';

import { ProviderInfoComponent } from './index';

export const ProviderInfoRoutes: Route[] = [
	{
		path: 'provider-info',
		component: ProviderInfoComponent,
	}
];
