import { Route } from '@angular/router';

import { CouponCollectedComponent } from './index';

export const CouponCollectedRoutes: Route[] = [
	{
		path: 'coupon-collected',
		component: CouponCollectedComponent,
	}
];
