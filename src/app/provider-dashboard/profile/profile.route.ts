import { Route } from '@angular/router';

import { ProviderProfileComponent } from './index';

export const ProviderProfileRoutes: Route[] = [
	{
		path: 'profile',
		component: ProviderProfileComponent,
	}
];
